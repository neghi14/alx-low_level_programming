#include "main.h"
/**
 * _atoi - function
 * @s: string to convert
 *
 * Description: function to convert a string to an integer
 * Return: 0
 */
int _atoi(char *s)
{
	int i = 0;

	while (s[i])
	{
		;
	}
	return (0);
}

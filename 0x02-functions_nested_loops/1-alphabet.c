#include <stdio.h>
#include "main.h"
/**
 * main -entry point
 *
 * print lowercase of all alphabets
 *
 * n - variable that store a char
 *
 * Return: Always 0
 */

int main(void)
{
	print_alphabet()
	{
		char c = 'a'

		while (c <= 'z')
		{
			_putchar(c);
			c++;
		}
		_putchar(10);
		return (0);
	}
	return (0);
}

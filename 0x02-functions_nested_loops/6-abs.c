#include "main.h"

/**
 *_abs - main entry
 *
 * @n: first operand
 *
 * Description: Computes the absolute value of an integer
 *
 * Return: always 0
 */
int _abs(int n)
{
	if (r > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
	return (0);
}

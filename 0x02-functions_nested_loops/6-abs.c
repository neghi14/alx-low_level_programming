#include "main.h"

/**
 *_abs - main entry
 *
 * @n: first operand
 *
 * Description: Computes the absolute value of an integer
 *
 * Return: always 0
 */
int _abs(int r)
{
	if (r > 0)
	{
		return (r)
	}
	else
	{
		return (-r)
	}
	return (0)
}
